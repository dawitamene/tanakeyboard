<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TanaKeyboard" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color (Tana palette). -->
        <item name="colorPrimary">@color/tana_deep</item>
        <item name="colorPrimaryVariant">@color/tana_deep</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/tana_lake</item>
        <item name="colorSecondaryVariant">@color/tana_lake</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Launch splash: brand surface + centered Tana logo as the window
         background. MainActivity.setTheme() swaps back to the theme above
         before Compose draws. -->
    <style name="Theme.TanaKeyboard.Splash" parent="Theme.TanaKeyboard">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/splash_status_bar</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
    </style>
</resources>