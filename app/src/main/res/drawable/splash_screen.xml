<?xml version="1.0" encoding="utf-8"?>
<!--
  Launch splash shown while the process starts (classic windowBackground
  technique, no extra dependency, works on all supported API levels).
  Brand surface + the centered Addiyon logo. MainActivity swaps back to the
  normal app theme in onCreate() before drawing Compose.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item
        android:width="160dp"
        android:height="160dp"
        android:drawable="@drawable/ic_addiyon_icon"
        android:gravity="center" />
</layer-list>
